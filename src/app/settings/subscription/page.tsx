import { FeatureShell } from '@/components/app/feature-shell';

export default function SettingsSubscriptionPage() {
  return (
    <FeatureShell
      eyebrow="Settings / Subscription"
      title="Subscription"
      description="Manage free vs premium plan features and billing status."
    />
  );
}
