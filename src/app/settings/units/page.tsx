import { FeatureShell } from '@/components/app/feature-shell';

export default function SettingsUnitsPage() {
  return (
    <FeatureShell
      eyebrow="Settings / Units"
      title="Units Preferences"
      description="Choose metric or imperial units for all physical data displays."
    />
  );
}
